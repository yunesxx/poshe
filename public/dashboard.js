const loginView=document.getElementById('loginView'),dashView=document.getElementById('dashView'),loginForm=document.getElementById('loginForm'),pwInput=document.getElementById('pwInput'),logoutBtn=document.getElementById('logoutBtn'),addForm=document.getElementById('addForm'),dropzone=document.getElementById('dropzone'),fileInput=document.getElementById('fileInput'),nameInput=document.getElementById('nameInput'),catInput=document.getElementById('catInput'),descInput=document.getElementById('descInput'),priceInput=document.getElementById('priceInput'),submitBtn=document.getElementById('submitBtn'),adminList=document.getElementById('adminList'),adminEmpty=document.getElementById('adminEmpty'),adminCount=document.getElementById('adminCount'),toast=document.getElementById('toast'),KEY='poshe_admin_pw';let pw=sessionStorage.getItem(KEY)||'',pickedFile=null;function showToast(m,e=false){toast.textContent=m;toast.className='toast show'+(e?' error':'');setTimeout(()=>toast.className='toast',2600)}function enterDash(){loginView.style.display='none';dashView.style.display='block';logoutBtn.style.display='inline-block';loadList()}function logout(){sessionStorage.removeItem(KEY);pw='';loginView.style.display='block';dashView.style.display='none';logoutBtn.style.display='none'}logoutBtn.addEventListener('click',e=>{e.preventDefault();logout()});loginForm.addEventListener('submit',async e=>{e.preventDefault();const c=pwInput.value.trim();try{const r=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:c})});if(!r.ok)throw new Error('كلمة السر غير صحيحة');pw=c;sessionStorage.setItem(KEY,pw);enterDash()}catch(err){showToast(err.message,true)}});function resizeImage(file,maxDim=1280,quality=.82){return new Promise((res,rej)=>{const img=new Image();img.onload=()=>{const s=Math.min(1,maxDim/Math.max(img.width,img.height));const w=Math.round(img.width*s),h=Math.round(img.height*s);const c=document.createElement('canvas');c.width=w;c.height=h;c.getContext('2d').drawImage(img,0,0,w,h);c.toBlob(b=>{if(!b)return rej(new Error('فشل ضغط الصورة'));const ext=b.type==='image/webp'?'.webp':'.jpg';const name=file.name.replace(/\.[^.]+$/,'')+ext;res(new File([b],name,{type:b.type}))},'image/webp',quality)};img.onerror=()=>rej(new Error('فشل قراءة الصورة'));img.src=URL.createObjectURL(file)})}async function pickFile(f){if(!f||!f.type.startsWith('image/'))return;try{pickedFile=f.size>400*1024?await resizeImage(f):f}catch{pickedFile=f}const u=URL.createObjectURL(pickedFile);dropzone.classList.add('has-file');dropzone.innerHTML=`<div class="preview"><img src="${u}" alt="preview"/></div>`}dropzone.addEventListener('click',()=>fileInput.click());fileInput.addEventListener('change',()=>pickFile(fileInput.files[0]));['dragover','dragenter'].forEach(ev=>dropzone.addEventListener(ev,e=>{e.preventDefault();dropzone.style.background='#fbdcec'}));['dragleave','drop'].forEach(ev=>dropzone.addEventListener(ev,e=>{e.preventDefault();dropzone.style.background=''}));dropzone.addEventListener('drop',e=>pickFile(e.dataTransfer.files[0]));addForm.addEventListener('submit',async e=>{e.preventDefault();if(!pickedFile)return showToast('اختاري صورة أولاً',true);const fd=new FormData();fd.append('image',pickedFile);fd.append('name',nameInput.value);fd.append('category',catInput.value);fd.append('description',descInput.value);fd.append('price',priceInput.value);submitBtn.disabled=true;submitBtn.textContent='جاري الرفع...';try{const r=await fetch('/api/cakes',{method:'POST',headers:{'x-admin-password':pw},body:fd});const d=await r.json();if(!r.ok)throw new Error(d.error||'فشل الرفع');showToast('تمت الإضافة بنجاح ✦');addForm.reset();pickedFile=null;dropzone.classList.remove('has-file');dropzone.innerHTML=`<div class="dropzone-prompt"><strong>اضغطي لاختيار صورة</strong><span>JPG / PNG / WEBP — حد أقصى 8MB</span></div>`;loadList()}catch(err){showToast(err.message,true)}finally{submitBtn.disabled=false;submitBtn.textContent='إضافة للقائمة'}});async function loadList(){const r=await fetch('/api/cakes',{cache:'no-store'});const d=await r.json();adminCount.textContent=`${d.length} صنف`;if(!d.length){adminList.innerHTML='';adminEmpty.style.display='block';return}adminEmpty.style.display='none';adminList.innerHTML=d.map(c=>`<div class="admin-card" data-id="${c.id}"><button class="del-btn" data-id="${c.id}" title="حذف">✕</button><img src="${c.image}" alt="" loading="lazy"/><div class="admin-card-body"><h4>${esc(c.name)}</h4><div class="meta">${esc(c.category)}${c.price?' · '+esc(c.price):''}</div></div></div>`).join('');adminList.querySelectorAll('.del-btn').forEach(b=>{b.addEventListener('click',async()=>{if(!confirm('حذف هذه الكيكة من القائمة؟'))return;try{const r=await fetch('/api/cakes/'+b.dataset.id,{method:'DELETE',headers:{'x-admin-password':pw}});if(!r.ok)throw new Error('فشل الحذف');showToast('تم الحذف');loadList()}catch(err){showToast(err.message,true)}})})}function esc(s){return String(s||'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}if(pw){fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:pw})}).then(r=>r.ok?enterDash():logout())}